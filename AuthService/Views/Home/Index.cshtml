@model AuthService.Data.Models.Views.Index
@{
    ViewData["Title"] = "Вход";
}

<div class="container">
    <form class="form-horizontal col-sm-5" action="#" method="post">
        <div class="form-center">
            @if(Model.Message != null) {
                <label class="control-label" style="text-align: center;" asp-for="Message">@Model.Message</label>
            }
            <div class="form-group">
                <label class="control-label col-sm-2" for="Login">Почта:</label>
                <input class="form-control" type="text" asp-for="Email"/>
            </div>
            <div class="form-group mt-1">
                <label class="control-label col-sm-2" for="Password">Пароль:</label>
                <input class="form-control" type="password" asp-for="Password"/>
            </div>
            <input class="btn btn-primary mt-1" type="submit" value="Вход">
            <br>
            <input class="btn btn-primary mt-1" type="button"
            value="Регистрация"
            onclick="location.href='@Url.Action("Register")'"/>
        </div>
    </form>
</div>
